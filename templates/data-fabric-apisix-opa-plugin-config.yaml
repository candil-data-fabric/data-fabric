apiVersion: apisix.apache.org/v2
kind: ApisixPluginConfig
metadata:
  name: data-fabric-apisix-opa-plugin-config
spec:
  plugins:
  - name: opa
    enable: true
    config:
      host: http://data-fabric-opa.default.svc.cluster.local:8181 # Point to OPA service.
      policy: aerOS
